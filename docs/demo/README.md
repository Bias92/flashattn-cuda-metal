# Old demo recordings

WMMA attention versus a PyTorch matmul/softmax expression.

`launch_demo.sh` runs `wmma_demo.py` and `naive_demo.py` in two tmux panes.

For the current comparison, use [compare_pytorch.py](../../bench/compare_pytorch.py).
